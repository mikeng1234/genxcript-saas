---
name: No redundant download buttons
description: User prefers single-action buttons — don't show separate Generate then Download buttons
type: feedback
---

When generating downloadable files (PDFs, reports), use a single button that both generates and downloads.
Don't show a "Generate" button followed by a separate "Download" button — the user finds this redundant.
Streamlit's st.download_button handles both in one click.
